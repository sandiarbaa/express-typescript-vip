export interface ProductType {
  product_id: string
  name: string
  price: number
  size: string
}

export interface UserType {
  user_id: string
  email: string
  name: string
  password: string
  role: string
}